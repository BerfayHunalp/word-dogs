Run full CI checks locally: lint, type-check, test, and build.

1. Run `npm run lint` — report and fix any lint errors
2. Run `npm run typecheck` — report and fix any type errors
3. Run `npm test` — report and fix any test failures
4. Run `npm run build` — confirm production build succeeds
5. Summarize results for each step
